{
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "all": "npm-run-all -p server client",
    "client": "cd Client && npm start",
    "server": "cd Server && npm start"
  }
}
