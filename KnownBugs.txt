known bugs:
- When "Show More" is clicked on a moneyline bet from 3 rows to the max number of rows, the spacing between the 2nd and 3rd row changes
- Getting multiple components updating at once in react console: react-dom.development.js:86 Warning: Cannot update a component (`Navbar`) while rendering a different component (`Account`). To locate the bad setState() call inside `Account`, follow the stack trace as described in
- Can submit bets even if balance is below submitted bets
- Balance is not removed from available balance upon bet submission
- If there is an error submitting bets (like too low of balance), user is not notified
